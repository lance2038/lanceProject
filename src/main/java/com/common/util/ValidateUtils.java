/*
package com.common.util;

import java.util.List;
import java.util.Map;

*/
/**
 * 验证工具类
 *
 * @author Aries.Y
 * @since 2008-12-1
 *//*

public class ValidateUtils {


    */
/**
     * 判断字符串是否为空或者空串, 全角/半角空格均视为空
     *
     * @param inStr
     * @return
     *
     * @author Aries.Y
     * @since 2008-12-1
     *//*

    public static boolean isNull(String inStr) {

        if (inStr == null || "".equals(CommonUtils.trimSpace(inStr))) {
            return true;
        }
        return false;
    }

    */
/**
     * 判断List是否为空
     *
     * @param list
     * @return
     *
     * @author Aries.Y
     * @since 2008-12-15
     *//*

    public static boolean isNull(List list) {
        if (list == null || list.size() < 1) {
            return false;
        }
        return true;
    }


    */
/**
     * 判断Map是否为空
     *
     * @param map
     * @return
     *
     * @author Aries.Y
     * @since 2008-12-15
     *//*

    public static boolean isNull(Map map) {
        if (map == null || map.size() < 1) {
            return false;
        }
        return true;

    }
}
*/
